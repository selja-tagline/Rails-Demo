<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form_with model: @post, data: { turbo: false } do |form| %>
  <%= form.label :name, "Post Title"%>
  <%= form.text_field :name, style: "display: block"%>
  <%= form.label :description, "Post Detail" %>
  <%= form.text_area :description, size: "50x5", style: "display: block; margin-bottom: 20px;" %>
  <%# <%= form.label :images, "Post Images" %>
  <%# <%= form.file_field :images, multiple: true, style: "display: block; margin-bottom: 20px;" %>
  <%= form.submit %>
  <% end %>  

<% end %>
gem 'jsonapi-serializer'
